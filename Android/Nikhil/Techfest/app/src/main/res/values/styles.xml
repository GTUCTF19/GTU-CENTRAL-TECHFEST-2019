<resources>

    <!--App Theme-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowAnimationStyle">@style/FadeAnimationTheme</item>
    </style>

    <style name="FadeAnimationTheme">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!--Toolbar Theme-->
    <style name="ToolbarTheme" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="ToolbarPopupTheme" parent="Theme.AppCompat.Light">
        <item name="android:selectableItemBackground">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!--Primary Ripple Theme-->
    <style name="PrimaryRipple" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/primaryRipple</item>
    </style>

</resources>
